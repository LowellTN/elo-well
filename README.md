# elo-well
A Java Spring based web app that aim to create bracket, manage player and their ELO ranking through tounaments, leagues and clubs.


## Set up 

## Launch app 

While waiting for docker to be set up, the application can be test by doing :

```bash
mvn clean compile exec:java
```